{
  "name": "claude-plugins",
  "private": true,
  "version": "0.0.0",
  "description": "通用 AI CLI 插件集（Claude Code / Codex 等环境）",
  "packageManager": "pnpm@11.1.3",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "dev": "pnpm -r --parallel run dev",
    "publish:vision-relay": "./scripts/publish-sync.sh vision-relay",
    "publish:commit-flow": "./scripts/publish-sync.sh commit-flow"
  },
  "dependencies": {
    "vision-relay": "^0.12.16"
  }
}
